query AllPrograms {
	programs: allPrograms(orderBy: startDate_DESC) {
		...ProgramFragment
	}
	pagination: _allProgramsMeta {
		count
	}
}

query Program($slug: String!) {
	program(filter: { slug: { eq: $slug } }) {
		...ProgramFragment
	}
}
